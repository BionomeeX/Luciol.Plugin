namespace Luciol.Plugin.Context.Annotation
{
    /// <summary>
    /// Annotations with high priority are displayed over annotations with low priority
    /// </summary>
    public enum Priority
    {
        Low,
        Normal,
        High
    }
}
